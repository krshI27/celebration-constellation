# Raw Data

This directory contains original, unprocessed data files as received from sources.

## Guidelines

- Never modify files in this directory
- Keep original file names when possible
- Document data sources and acquisition dates
- Include metadata files when available

## File Organization

- Use descriptive naming: `source_dataset_YYYY-MM-DD.ext`
- Group related files in subdirectories if needed
- Include source documentation where possible
